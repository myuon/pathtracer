{
  "name": "pathtracer",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "bench": "node bench/run.mjs run",
    "bench:ref": "node bench/run.mjs ref"
  },
  "devDependencies": {
    "@webgpu/types": "^0.1.60",
    "playwright-core": "^1.62.1",
    "typescript": "^5.7.3",
    "vite": "^6.0.11"
  }
}
